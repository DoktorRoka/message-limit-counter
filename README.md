# SillyTavern message limit counter

Counts requests to the provider and warns the user if they are near the limit. Comes with a cooldown timer that resets after two minutes

Everything is configurable

